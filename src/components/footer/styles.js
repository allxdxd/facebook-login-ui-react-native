import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  separator: {
    alignSelf: 'center',
    paddingHorizontal: 5,
    fontSize: 15,
    color: '#A7A8AB',
    background: "#fff",
  },
  create: {
    background: '#fff',
    marginTop: 20,
    flex: 1,
    padding: 0,
    marginHorizontal: 60,
  },
});

export default styles;
